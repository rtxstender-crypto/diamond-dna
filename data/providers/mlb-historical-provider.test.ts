import {describe,expect,it} from "vitest";
import {parseSeasonSplit} from "./mlb-historical-provider";
describe("MLB historical parser",()=>{it("parses sourced values and derives rates",()=>{const parsed=parseSeasonSplit({season:"2000",player:{id:123,fullName:"Verified Player"},team:{name:"Verified Club"},stat:{age:28,gamesPlayed:150,plateAppearances:600,avg:".280",obp:".360",slg:".500",ops:".860",homeRuns:25,rbi:88,stolenBases:10,baseOnBalls:60,strikeOuts:120}},"hitter");expect(parsed).toMatchObject({playerId:"123",season:2000,ops:.86,walkRate:10,strikeoutRate:20,war:null,opsPlus:null});expect(parsed?.provenance.source).toBe("MLB Stats API")});it("rejects malformed responses",()=>expect(parseSeasonSplit({player:{}},"hitter")).toBeNull());it("preserves unsupported values as null",()=>{const parsed=parseSeasonSplit({season:1999,player:{id:1,fullName:"Pitcher"},stat:{gamesPitched:40,gamesStarted:0,inningsPitched:"50.0"}},"pitcher");expect(parsed?.fip).toBeNull();expect(parsed?.eraPlus).toBeNull();expect(parsed?.war).toBeNull()})});
