import{describe,expect,it}from"vitest";import{filterPlayerIndex,findIndexedPlayer,type ActivePlayerIndexRecord}from"./models/player-index";import{parseActivePlayerIndex}from"./providers/mlb-player-index-provider";
const people={people:[{id:1,fullName:"Garrett Crochet",currentAge:27,active:true,primaryPosition:{type:"Pitcher",abbreviation:"P"},batSide:{description:"Left"},pitchHand:{description:"Left"}},{id:1,fullName:"Garrett Crochet",active:true,primaryPosition:{type:"Pitcher",abbreviation:"P"}},{id:2,fullName:"Mookie Betts",currentAge:33,active:true,primaryPosition:{type:"Infielder",abbreviation:"SS"},batSide:{description:"Right"},pitchHand:{description:"Right"}}]};
const teams={teams:[{id:10,name:"Boston Red Sox",abbreviation:"BOS"},{id:20,name:"Los Angeles Dodgers",abbreviation:"LAD"}]};const rosters=[{roster:[{person:{id:1},parentTeamId:10,position:{abbreviation:"P"},status:{description:"Active"}}]},{roster:[{person:{id:2},parentTeamId:20,position:{abbreviation:"SS"},status:{description:"Active"}}]}];const index=parseActivePlayerIndex(people,teams,rosters);const extended:ActivePlayerIndexRecord[]=index.map(p=>({...p,pitcherRole:p.role==="pitcher"?"Starter":null}));
describe("active MLB player index",()=>{
 it("parses roster identity and removes duplicate IDs",()=>{expect(index).toHaveLength(2);expect(index[0]).toMatchObject({mlbId:1,name:"Garrett Crochet",teamAbbreviation:"BOS",role:"pitcher",imageUrl:null})});
 it("supports partial and case-insensitive search",()=>expect(filterPlayerIndex(extended,{query:"CROCH"}).map(p=>p.name)).toEqual(["Garrett Crochet"]));
 it("filters team, position, role, and pitcher role",()=>{expect(filterPlayerIndex(extended,{team:"LAD"})).toHaveLength(1);expect(filterPlayerIndex(extended,{position:"SS"})[0].name).toBe("Mookie Betts");expect(filterPlayerIndex(extended,{role:"pitcher",pitcherRole:"Starter"})).toHaveLength(1)});
 it("looks up profiles by MLB ID",()=>{expect(findIndexedPlayer(index,2)?.name).toBe("Mookie Betts");expect(findIndexedPlayer(index,999)).toBeNull()});
 it("fails closed on malformed provider payloads",()=>{expect(parseActivePlayerIndex(null,teams,rosters)).toEqual([]);expect(parseActivePlayerIndex(people,null,rosters)).toEqual([])});
});
