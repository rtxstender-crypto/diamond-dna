import{describe,expect,it}from"vitest";import{parseTradeRequest}from"./intent";
describe("trade request parsing",()=>{it("identifies team and target",()=>{const i=parseTradeRequest("I'm the Red Sox. Build me a trade for Elly De La Cruz.");expect(i.acquiringTeamId).toBe(111);expect(i.targetName).toBe("Elly De La Cruz")});it("preserves constraints",()=>{const i=parseTradeRequest("Give me a three-player prospect-heavy package for Paul Skenes. Don't include Roman Anthony.");expect(i.packageSize).toBe(3);expect(i.style).toBe("prospect-heavy");expect(i.excludedNames).toContain("Roman Anthony")});it("recognizes MLB-ready requests",()=>expect(parseTradeRequest("Build a win-now trade for Elly De La Cruz").style).toBe("mlb-ready"))});
