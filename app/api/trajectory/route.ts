import { NextRequest, NextResponse } from "next/server";
import { compareAndRank, type HistoricalYearFilter, type TrajectoryRole } from "@/data/trajectory-comparison";
import { fetchCareer, fetchHistoricalPool, resolvePlayer, toCareerSeries } from "@/data/providers/mlb-historical-provider";

export async function GET(request:NextRequest){try{const p=request.nextUrl.searchParams,name=p.get("name"),role=p.get("role") as TrajectoryRole,filter=(p.get("filter")??"2020s") as HistoricalYearFilter,specific=Number(p.get("year"));if(!name||!["hitter","pitcher"].includes(role))return NextResponse.json({error:"Invalid request"},{status:400});const identity=await resolvePlayer(name);const rawCareer=await fetchCareer(identity.id,role);const currentCareer=rawCareer.map(season=>({...season,position:identity.position}));const requested=Number(p.get("season"));const current=currentCareer.find(s=>s.season===requested)??currentCareer.at(-1);if(!current)return NextResponse.json({error:"No MLB season data is available for this player."},{status:404});const pool=await fetchHistoricalPool(role,filter,Number.isFinite(specific)?specific:undefined);const preliminary=compareAndRank(current,pool,new Map(),3);const colors=["#c8f75b","#5ea9ff","#f5a75b"];const careerPairs=await Promise.all(preliminary.map(async(c,index)=>[c.historicalSeason.playerId,toCareerSeries(await fetchCareer(c.historicalSeason.playerId,role),colors[index])] as const));const comparisons=compareAndRank(current,pool,new Map(careerPairs),3);return NextResponse.json({source:"MLB Stats API",coverage:"1950–2025",identity,currentCareer,currentCareerSeries:toCareerSeries(currentCareer,"#d3ff62"),currentSeason:current,comparisons,candidateCount:pool.length});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"MLB data unavailable"},{status:502})}}
