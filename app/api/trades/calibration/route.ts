import{NextResponse}from"next/server";import{getPlayerDataProvider}from"@/data/player-service";import{fetchProspectDataset}from"@/data/providers/mlb-milb-provider";import{rankMlb,rankProspects}from"@/data/trade-value/calibration";
export async function GET(){try{const[mlb,milb]=await Promise.all([getPlayerDataProvider().listPlayers(),fetchProspectDataset()]);return NextResponse.json({generatedAt:new Date().toISOString(),limitations:["MLB rankings are provisional because the current provider lacks verified contract/control data and live three-season hydration.","Prospect rankings are statistical only and do not ingest external scouting rankings."],mlb:rankMlb(mlb),prospects:rankProspects(milb.players)},{headers:{"cache-control":"private, max-age=300"}})}catch{return NextResponse.json({error:"Calibration data unavailable."},{status:503})}}
