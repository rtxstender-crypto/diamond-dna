import{PlayerDirectory}from"@/components/player-directory";import{Eyebrow}from"@/components/ui";import{getActivePlayerDirectory}from"@/data/player-index-service";
export default async function PlayersPage(){const players=await getActivePlayerDirectory().catch(()=>null);return <div className="subpage page-width players-page"><header className="page-header"><div><Eyebrow>ACTIVE MLB DIRECTORY</Eyebrow><h1>Player <em>Index</em></h1><p>Search every active Major League roster in one place.</p></div>{players&&<div className="updated"><span>INDEX STATUS</span><strong>{players.length} PLAYERS</strong><small>● MLB DATA</small></div>}</header>{players?<PlayerDirectory players={players}/>:<div className="empty-state">The live MLB player index is temporarily unavailable. Please try again shortly.</div>}</div>}
