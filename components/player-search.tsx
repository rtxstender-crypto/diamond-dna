"use client";import{useState}from"react";import Link from"next/link";import{Search}from"lucide-react";import type{IndexedPlayer}from"@/data/player-index-service";
export function PlayerSearch({players}:{players:IndexedPlayer[]}){const[query,setQuery]=useState("");const matches=query.trim().length>=2?players.filter(p=>p.name.toLowerCase().includes(query.toLowerCase())).slice(0,6):[];return <div className="home-player-search"><div><span>PLAYER DATABASE</span><strong>Search any active MLB player</strong></div><label><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Try “croch”..." aria-label="Search active MLB players"/></label>{matches.length>0&&<div className="home-search-results">{matches.map(player=><Link key={player.mlbId} href={`/players/${player.mlbId}`}><span>{player.name}</span><small>{player.teamAbbreviation} · {player.position}</small></Link>)}</div>}</div>}
